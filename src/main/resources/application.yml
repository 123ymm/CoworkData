spring:
  application:
    name: CoworkData
  datasource:
    url: jdbc:postgresql://localhost:5432/coworkdata
    username: cowork
    password: cowork123
    driver-class-name: org.postgresql.Driver
  # 删掉原来的 autoconfigure.exclude（那几行 DataSource / MybatisPlus 排除）